create database SPS;

use SPS;

CREATE TABLE users (
  Username varchar(255) NOT NULL,
  User email varchar(255) NOT NULL,
  User Password varchar(255) NOT NULL,
  User Phone_Number int(10) NOT NULL,
  Last_name varchar(50) NOT NULL,
  Surname varchar(50) NOT NULL,
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE users_vehicle (
  Username varchar(255) NOT NULL,
  Vehicle_Model varchar(255) NOT NULL,
  Vehicle_Plates varchar(255) NOT NULL
)
ALTER TABLE users
  ADD PRIMARY KEY (Username);
ALTER TABLE users ADD FULLTEXT KEY Username (Username);
COMMIT;
ALTER TABLE user vehicle
  ADD PRIMARY KEY (Username);
COMMIT;

create table phone_numbers(email varchar(50) not null,phone_number varchar(30), primary key(email,Phone_Number),
constraint c6 foreign key(email) references users(email) on delete cascade on update cascade)engine='innodb';


insert into users values('monopanagis','monopanagis@gmail.com','6969696969',Panagis,'Kwsths');

insert into users values('edwmakris','makris@gmail.com','6998765432',Makris,'Giwrgos');

insert into users values('alexalex','alex@gmail.com','6923456789',Chrys,'Alex');

insert into users_vehicle values('edwmakris','mini couper s','aaa 1111');

insert into users_vehicle values('alexalex','audi r8','cba 9876');

insert into users_vehicle values('monopanagis','aston martin valkayry','aek 1924');

insert into users_vehicle values('monopanagis','ferrari laferarri','pao 1023');

